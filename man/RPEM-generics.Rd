% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generics.R
\name{RPEM-generics}
\alias{RPEM-generics}
\alias{generics}
\alias{nedge}
\alias{edge}
\alias{edge<-}
\alias{edgenames}
\alias{edgenames<-}
\alias{geometry}
\alias{geometry<-}
\alias{labels<-}
\alias{as.graph}
\alias{set.origin}
\alias{crosslink}
\alias{locate}
\alias{evolution.model}
\alias{vcv}
\title{Generic Functions and Methods for RPEM}
\usage{
nedge(x)

edge(x)

edge(x) <- value

edgenames(x)

edgenames(x) <- value

geometry(x)

geometry(x) <- value

labels(object) <- value

as.graph(x, ...)

set.origin(x, v, ...)

crosslink(x, link, ...)

locate(x, target, ...)

evolution.model(object, y, ..., x = NULL)

vcv(x, ...)
}
\arguments{
\item{x}{A \code{\link{graph-class}} object, or a numeric vector/matrix of
auxiliary traits used in evolutionary model estimation. If used as a trait,
default is \code{NULL} (no auxiliary traits).}

\item{value}{For assignment methods: a two-column \code{data.frame} of
coordinates (e.g., x, y) to assign to a graph; or new value appropriate to
the method.}

\item{object}{A \code{\link{graph-class}} object or any object that can be
converted to one.}

\item{...}{Additional arguments passed to or from methods, depending on the
class and implementation.}

\item{v}{A character string (vertex name) or integer (index) specifying the
origin (root) vertex.}

\item{link}{A data frame containing information to add cross-links (e.g.,
reticulation edges) to a graph.}

\item{target}{A numeric or character vector specifying one or more target
species (vertices) in graph \code{x}, typically for placement or prediction.}

\item{y}{A numeric vector or matrix of response trait(s) used to estimate
parameters in evolutionary models (e.g., in \code{evolution.model}).}
}
\value{
The return value depends on the specific method and class. For
example:
\itemize{
  \item \code{nedge()}: integer scalar (number of edges),
  \item \code{edge()}: list or data structure representing edges,
  \item \code{as.graph()}: an object of class \code{"graph"},
  \item \code{evolution.model()}: a list with estimated parameters and fit
  statistics.
}
}
\description{
Defines a set of S3 generic functions used internally and
externally by the \pkg{RPEM} package for graph manipulation, phylogenetic
modeling, and trait evolution analysis. These methods support the package's
object-oriented design, particularly around the \code{\link{graph-class}} and
\code{PEM-class} objects.

Users typically interact with these generics through their methods on
specific classes (e.g., \code{"graph"}, \code{"PEM"}), rather than calling
the generics directly.

Access the number of edges in a graph-like object.

Extract the edges of a graph-like object as a list or data
structure.

Replace the edges of an object with a new set of edges.

Assign names (labels) to the edges of a graph-like object.

Obtain the geometric coordinates (e.g., x, y) associated with an
object.

Assign geometric coordinates to an object, such as spatial
layout for plotting.

Assign labels (e.g., species names) to the vertices of an
object.

Convert an object (e.g., phylo, network) into a
\code{\link{graph-class}} object.

Designate a specific vertex as the origin (root) of the graph.

Add reticulation edges (e.g., hybridization, lateral transfer)
to a graph.

Determine the phylogenetic placement (LCA and distance) of
target species in a graph.

Estimate the parameters of a trait evolution model (e.g.,
steepness, rate) using response and auxiliary traits on a PEM-class object.

Compute the variance-covariance matrix of an object, typically
reflecting phylogenetic or evolutionary structure.
}
\details{
This file declares S3 generics and associated method dispatchers for key
operations in \pkg{RPEM}, including:
\itemize{
  \item Graph structure access and modification (e.g., \code{nedge},
  \code{edge}, \code{edgenames}),
  \item Geometry and spatial layout handling (e.g., \code{geometry},
  \code{geometry<-}),
  \item Graph conversion and labeling (e.g., \code{as.graph},
  \code{labels<-}),
  \item Phylogenetic operations (e.g., \code{locate}, \code{set.origin},
  \code{crosslink}),
  \item Evolutionary model estimation (e.g., \code{evolution.model},
  \code{vcv}).
}
}
\section{Functions}{
\itemize{
\item \code{nedge()}: 

\item \code{edge()}: 

\item \code{edge(x) <- value}: 

\item \code{edgenames()}: Edge Names Extraction

A method to extract edge names from an object.

\item \code{edgenames(x) <- value}: 

\item \code{geometry()}: 

\item \code{geometry(x) <- value}: 

\item \code{labels(object) <- value}: 

\item \code{as.graph()}: 

\item \code{set.origin()}: 

\item \code{crosslink()}: 

\item \code{locate()}: 

\item \code{evolution.model()}: 

\item \code{vcv()}: 

}}
\author{
\packageAuthor{RPEM}
}
\keyword{internal}
\keyword{methods}
